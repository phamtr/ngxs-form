import { AfterViewInit, ElementRef, EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { EventRegistry } from '@angular-mdc/web/common';
import { MdcSnackbarConfig } from './snackbar-config';
export declare class MdcSnackbarComponent implements AfterViewInit, OnInit, OnDestroy {
    elementRef: ElementRef;
    private _registry;
    data: {
        message: string;
        actionText: string;
    };
    config: MdcSnackbarConfig;
    isHostClass: boolean;
    ariaLive: string;
    ariaAtomic: string;
    ariaHidden: string;
    snackText: ElementRef;
    actionButton: ElementRef;
    /** Event emitted when the snackbar is shown. */
    readonly shown: EventEmitter<void>;
    /** Event emitted when the snackbar is dismissed. */
    readonly dismissed: EventEmitter<void>;
    private _mdcAdapter;
    private _foundation;
    constructor(elementRef: ElementRef, _registry: EventRegistry);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    setDismissOnAction(dismissOnAction: boolean): void;
    private _getSnackText();
    private _getActionButton();
    /** Retrieves the DOM element of the component host. */
    private _getHostElement();
}
