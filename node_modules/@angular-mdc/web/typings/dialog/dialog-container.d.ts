import { ComponentRef, ElementRef } from '@angular/core';
import { Subject } from 'rxjs';
import { BasePortalOutlet, ComponentPortal, CdkPortalOutlet } from '@angular-mdc/web/portal';
import { MdcDialogConfig } from './dialog-config';
export declare class MdcDialogContainer extends BasePortalOutlet {
    protected _elementRef: ElementRef;
    private _document;
    _portalOutlet: CdkPortalOutlet;
    config: MdcDialogConfig;
    constructor(_elementRef: ElementRef, _document: any);
    closed(): void;
    /** A subject emitting after the dialog exits the view. */
    _afterExit: Subject<void>;
    /**
     * Attach a ComponentPortal as content to this dialog container.
     * @param portal Portal to be attached as the dialog content.
     */
    attachComponentPortal<T>(portal: ComponentPortal<T>): ComponentRef<T>;
}
